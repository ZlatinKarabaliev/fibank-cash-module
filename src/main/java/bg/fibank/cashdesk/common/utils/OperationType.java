package bg.fibank.cashdesk.common.utils;

/**
 * Enum representing supported types of cash operations.
 */
public enum OperationType {
    /**
     * Deposit cash into the system.
     */
    DEPOSIT,

    /**
     * Withdraw cash from the system.
     */
    WITHDRAWAL
}
